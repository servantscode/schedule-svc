apiVersion: apps/v1
kind: Deployment
metadata:
  name: schedule-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: schedule-svc
  template:
    metadata:
      labels:
        app: schedule-svc
    spec:
      containers:
      - name: schedule-svc
        image: servantscode/schedule-svc
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "200Mi"
        env:
        - name: ES_JAVA_OPTS 
          value: "-Xmx128m -Xms128m"
---
kind: Service
apiVersion: v1
metadata:
  name: schedule-svc
spec:
  selector:
    app: schedule-svc
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
